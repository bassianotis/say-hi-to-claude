services:
  claude-work:
    build: .
    container_name: claude-work-ping
    restart: unless-stopped
    environment:
      TZ: ${TZ:-UTC}
      NOTIFY_URL: ${NOTIFY_URL:-}
    volumes:
      - ${CLAUDE_WORK_AUTH:-./claude-auth}:/root/.claude
